/**
 * @type {import('prettier').Config}
 */
const config = {
    tabWidth: 4,
    printWidth: 100,
    singleQuote: true,
    quoteProps: 'consistent',
    arrowParens: 'avoid',
    bracketSpacing: false,
};

export default config;
